services:
  php:
    build:
      context: ./docker/php
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      COMPOSER_HOME: /tmp/composer
    volumes:
      - .:/app
    working_dir: /app
    command: ["tail", "-f", "/dev/null"]
